[project]
name = "panelizer-cli"
version = "0.1.0"
description = "A CLI tool for carousel panel layout"
requires-python = ">=3.11"
dependencies = [
    "textual>=0.40.0",
    "pillow>=10.0.0",
    "rich>=13.0.0"
]